<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.shelfflow.mapper.OrderDetailMapper">
    <insert id="insertBatch" parameterType="com.shelfflow.entity.OrderDetail">
        INSERT INTO order_detail(name, image, order_id, product_id, bundle_id, batch_id, product_spec, number, amount)
        VALUES
        <foreach collection="orderDetails" item="od" separator=",">
            (#{od.name}, #{od.image}, #{od.orderId}, #{od.productId}, #{od.bundleId}, #{od.batchId}, #{od.productSpec}, #{od.number}, #{od.amount})
        </foreach>
    </insert>

    <select id="getTop10ByMap" parameterType="java.util.Map" resultType="com.shelfflow.dto.ProductFlowDTO">
        select od.name as name , sum(od.number) as number from order_detail od
        inner join orders o
        on od.order_id = o.id
        <where>
            <if test="beginTime != null">
                and o.order_time &gt;= #{beginTime}
            </if>
            <if test="endTime != null">
                and o.order_time &lt;= #{endTime}
            </if>
            <if test="status != null">
                and o.status = #{status}
            </if>
        </where>
        group by name
        order by number desc
        limit 0,10
    </select>

</mapper>
